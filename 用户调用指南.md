# PDF 转 Markdown 服务用户调用指南

## 简介

本文档提供了如何使用 xDAN Smart OCR PDF 转 Markdown 服务的详细指南。该服务可以将 PDF 文件转换为结构化的 Markdown 格式，支持多语言文本和数学公式识别。

## 前提条件

使用本服务前，您需要：

1. 获取 API 密钥（请联系管理员）
2. 准备需要转换的 PDF 文件

## 调用方式

### 方式一：使用测试脚本（推荐）

我们提供了一个简单的 Python 脚本，可以帮助您快速测试和使用 API 服务。

#### 步骤 1：下载测试脚本

如果您已经有了 `test_pdf_api.py` 脚本，可以跳过此步骤。

#### 步骤 2：安装依赖

```bash
pip install requests
```

#### 步骤 3：运行脚本

```bash
python test_pdf_api.py --pdf 您的PDF文件路径.pdf --url http://159.54.182.15:8080 --api-key 您的API密钥 --output 输出文件.md
```

参数说明：

| 参数 | 描述 | 是否必需 |
|--------|------|--------|
| `--pdf` | PDF 文件路径 | 是 |
| `--url` | API 服务地址，默认为 http://localhost:8000 | 否 |
| `--api-key` | API 密钥 | 是 |
| `--output` | 输出 Markdown 文件路径，如不指定则直接打印到控制台 | 否 |
| `--timeout` | 等待转换完成的超时时间（秒），默认 300 秒 | 否 |
| `--interval` | 检查转换状态的间隔时间（秒），默认 5 秒 | 否 |

#### 示例

```bash
python test_pdf_api.py --pdf 文档.pdf --url http://159.54.182.15:8080 --api-key pgMePwetA3zidEixgzieOCrKHwGRps61cqufII_VJmY --output 文档.md
```

### 方式二：直接调用 API

如果您希望在自己的应用程序中集成 PDF 转 Markdown 功能，可以直接调用 API。

#### 方法 A：上传 PDF 文件

```python
import requests

# API 地址和密钥
api_url = "http://159.54.182.15:8080/convert"
api_key = "您的API密钥"

# 准备请求头和文件
headers = {"X-API-Key": api_key}
files = {"file": open("文档.pdf", "rb")}

# 发送请求
response = requests.post(api_url, headers=headers, files=files)

# 解析响应
if response.status_code == 200:
    task_id = response.json()["task_id"]
    print(f"任务 ID: {task_id}")
else:
    print(f"错误: {response.status_code}, {response.text}")
```

#### 方法 B：通过 URL 下载并转换 PDF 文件

如果您的 PDF 文件已经有公开的 URL 地址，可以直接提供 URL，无需先下载文件。这种方式特别适合处理公开论文或文档。

```python
import requests

# API 地址和密钥
api_url = "http://159.54.182.15:8080/convert-url"
api_key = "CKa2PINwE9Lz9hKevrdWIe_KIV-sOWrektidwr6p7so"  # 请替换为您的API密钥

# 准备请求头和数据
headers = {
    "X-API-Key": api_key,
    "Content-Type": "application/json"
}

# 示例使用一个公开的学术论文 PDF URL
data = {"url": "https://proceedings.neurips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf"}

# 发送请求
response = requests.post(api_url, json=data, headers=headers)

# 解析响应
if response.status_code == 200:
    result = response.json()
    task_id = result["task_id"]
    print(f"任务 ID: {task_id}")
    print(f"初始状态: {result['status']}")
else:
    print(f"错误: {response.status_code}, {response.text}")
```

您也可以使用 curl 命令进行测试：

```bash
curl -X POST -H "Content-Type: application/json" -H "X-API-Key: CKa2PINwE9Lz9hKevrdWIe_KIV-sOWrektidwr6p7so" -d '{"url":"https://proceedings.neurips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf"}' http://159.54.182.15:8080/convert-url
```

#### 步骤 2：查询转换状态

```python
import time

# 查询状态 API 地址
status_url = f"http://159.54.182.15:8080/status/{task_id}"

# 循环查询直到完成
while True:
    # 发送请求
    response = requests.get(status_url, headers=headers)
    
    # 解析响应
    if response.status_code == 200:
        status_data = response.json()
        status = status_data["status"]
        
        if status == "completed":
            # 获取 Markdown 内容
            markdown = status_data["markdown"]
            print("转换完成!")
            
            # 保存到文件
            with open("文档.md", "w", encoding="utf-8") as f:
                f.write(markdown)
            break
            
        elif status == "failed":
            # 获取错误信息
            error = status_data["error"]
            print(f"转换失败: {error}")
            break
            
        else:
            print("正在处理中...")
            time.sleep(5)  # 等待 5 秒后再次查询
    else:
        print(f"错误: {response.status_code}, {response.text}")
        break
```

### 方式三：使用 cURL 命令行

如果您熟悉命令行操作，也可以使用 cURL 工具调用 API。

#### 步骤 1：上传 PDF 文件

```bash
curl -X POST \
  "http://159.54.182.15:8080/convert" \
  -H "X-API-Key: 您的API密钥" \
  -F "file=@文档.pdf" \
  -H "accept: application/json"
```

成功后会返回类似以下内容：

```json
{"task_id": "task_123456"}
```

#### 步骤 2：查询转换状态

```bash
curl -X GET \
  "http://159.54.182.15:8080/status/task_123456" \
  -H "X-API-Key: 您的API密钥" \
  -H "accept: application/json"
```

## 常见问题

### 1. 如何获取 API 密钥？

请联系系统管理员获取 API 密钥。

### 2. 支持哪些类型的 PDF？

支持大多数标准 PDF 文件，包括扫描件和数字 PDF。文件大小限制为 100MB。

### 3. 转换需要多长时间？

转换时间取决于 PDF 文件的大小、页数和复杂度。一般来说，几页的简单文档可能只需几秒钟，而大型复杂文档可能需要几分钟。

### 3. 如何处理转换错误？

如果转换失败，API 会返回错误信息。常见错误包括：
- 文件格式不正确
- 文件过大
- 服务器繁忙
- URL 无法访问或不指向 PDF 文件

请根据错误信息调整您的请求或稍后重试。

### 5. 支持哪些语言？

该服务支持 90+ 种语言的文本识别，包括中文、英文、日文等主要语言。

### 6. 数学公式如何处理？

数学公式会被识别并转换为 LaTeX 格式，在 Markdown 中以 `$` 或 `$$` 包围。

## 联系支持

如果您在使用过程中遇到任何问题，请联系技术支持团队：

- 邮箱：support@example.com
- 电话：+86 123 4567 8910
